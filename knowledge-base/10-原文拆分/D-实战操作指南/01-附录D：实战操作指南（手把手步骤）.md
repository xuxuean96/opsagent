---
type: reference
component: DDB
scenario: 01-附录D：实战操作指南（手把手步骤）
priority: P2
tags:
- DDB
- 01-附录D：实战操作指南（手把手步骤）
- reference
status: active
---

## 附录 D：实战操作指南（手把手步骤）

> 本章节提供具体场景的操作命令和步骤，可直接复制执行�?
### D.1 查看 DDB 当前有哪些配�?
**方法 1：查�?XML 配置文件中的处理配置名称**
```powershell
# Windows PowerShell
[xml]$xml = Get-Content "D:\test\DDB(CATIA).xml" -Encoding GB2312
$xml.配置集合.处理配置.处理配置 | ForEach-Object { $_.名称 }
# 输出示例：属性对�?/ 组装结构�?/ 属性合�?```

**方法 2：查�?DDBExec.ini 中的配置�?*
```ini
; 每个 [段名] 就是一个配�?[default]          �?配置�?
Mode=OutXml
DataType=CATIA
ConfigFile=D:\test\DDB(CATIA).xml

[autocad]          �?配置�?
Mode=OutXml
DataType=AUTOCAD_2D
ConfigFile=D:\test\AutoCAD_2D.xml

[签字]             �?配置�?
Mode=Update
DataType=PROTEL99_2D
```

**方法 3：用测试�?GUI 查看（推荐）**
1. 启动：`DDBExec.exe \t`
2. �?选择方案"下拉�?�?列出所有保存的方案
3. �?选择处理模式" �?OutXml/Update/Cable/Annot/Topdf
4. �?选择数据源类�? �?CATIA/PROE/AUTOCAD_2D/...
5. �?数据源读取配置文�? �?当前 XML 文件路径

**方法 4：查看日志中的配置加载信�?*
```
# 打开 KmDdbLog.Txt，搜�?"AddCon" �?"配置"
[1] 进入DDBDispatch.AddCon
[2] 配置集合 支持的数据源类型="CATIA;CATIA_2D" 配置版本="1"
[3] 系统配置 名称="扩展部件"
[4] 接口=DataPsr.CommonDataPsr
[5] 接口=CATIATranslator.CATIARW
```

---

### D.2 加密�?Java 客户端完整使用指�?
#### 前提条件
- Java 8+ 已安�?- `KmEncryptCardClient-1.0.3-fat.jar` 已下�?- 加密卡服务端已启动（Windows/Linux�?- 知道服务�?IP 和端口（默认 5600�?
#### 步骤 1：准备配置文�?
创建 `config.properties`�?```properties
server.ip=192.168.1.100
server.port=5600
soft.id=4
permit.number=1
mode=session
output.json=true
verbose=true
```

#### 步骤 2：基本测试命�?
```bash
# 测试连接（获取所有产品信息）
java -jar KmEncryptCardClient-1.0.3-fat.jar -s 192.168.1.100 -p 5600 --soft-id 4 --json

# 指定产品信息位掩码（7=全部信息�?java -jar KmEncryptCardClient-1.0.3-fat.jar -s 192.168.1.100 -p 5600 --soft-id 4 --product-info 7 --json

# 长连接模式（保持连接，定期获取信息）
java -jar KmEncryptCardClient-1.0.3-fat.jar -s 192.168.1.100 -p 5600 --soft-id 4 -m long --max-connection-time 3600

# 使用配置文件
java -jar KmEncryptCardClient-1.0.3-fat.jar -c config.properties

# 交互式配�?java -jar KmEncryptCardClient-1.0.3-fat.jar -i
```

#### 步骤 3：验证连接成�?
成功输出示例�?```json
{
  "success": true,
  "duration_ms": 1234,
  "result": {
    "success": true,
    "serverIp": "192.168.1.100",
    "serverPort": 5600,
    "softId": 4,
    "connected": true,
    "loggedIn": true,
    "customerName": "某某公司",
    "licenseCode": "XXXX-XXXX",
    "licenseCount": 1,
    "softwareCount": 1,
    "softwareList": [
      {
        "id": 4,
        "name": "KMPDM",
        "version": 1,
        "maxUsers": 100,
        "usedNodes": 5,
        "validity": "2026-12-31",
        "moduleList": "模块1;模块2"
      }
    ]
  }
}
```

#### 步骤 4：常见测试场�?
```bash
# 场景 1：测�?KMPDM (soft.id=4) 是否可用
java -jar KmEncryptCardClient.jar -s 192.168.1.100 -p 5600 --soft-id 4 --json
# 如果返回 usedNodes < maxUsers，说明还有授权节�?
# 场景 2：测�?KMCloud (soft.id=31) 是否可用
java -jar KmEncryptCardClient.jar -s 192.168.1.100 -p 5600 --soft-id 31 --json

# 场景 3：长连接稳定性测试（连接 1 小时�?java -jar KmEncryptCardClient.jar -s 192.168.1.100 -p 5600 --soft-id 4 -m long --max-connection-time 3600 --heartbeat-interval 30000

# 场景 4：并发压力测试（模拟 10 个客户端同时连接�?# 开 10 个命令行窗口，同时执行：
java -jar KmEncryptCardClient.jar -s 192.168.1.100 -p 5600 --soft-id 4
```

#### 步骤 5：常�?CLI 参数速查

| 参数 | 说明 | 示例 |
|------|------|------|
| `-s, --server <IP>` | 服务�?IP | `-s 192.168.1.100` |
| `-p, --port <port>` | 端口 | `-p 5600` |
| `--soft-id <ID>` | 软件 ID | `--soft-id 4` |
| `--permit-number <num>` | 许可证号 | `--permit-number 1` |
| `--product-info <num>` | 产品信息位掩�?| `--product-info 7` |
| `-m, --mode <session\|long>` | 连接模式 | `-m long` |
| `--json` | JSON 输出 | `--json` |
| `-v, --verbose` | 详细输出 | `-v` |
| `--max-connection-time <sec>` | 长连接最大时�?| `--max-connection-time 3600` |
| `--heartbeat-interval <ms>` | 心跳间隔 | `--heartbeat-interval 30000` |
| `--heartbeat-timeout <ms>` | 心跳超时 | `--heartbeat-timeout 35000` |
| `-c, --config <file>` | 配置文件 | `-c config.properties` |
| `-i, --interactive` | 交互�?| `-i` |
| `--connect-timeout <ms>` | 连接超时 | `--connect-timeout 15000` |
| `--request-timeout <ms>` | 请求超时 | `--request-timeout 30000` |
| `--max-reconnect <count>` | 最大重�?| `--max-reconnect 5` |
| `--reconnect-delay <ms>` | 重连延迟 | `--reconnect-delay 2000` |

---

### D.3 Linux 加密卡服务端起不�?- 完整排查

#### 步骤 1：确认进程是否真的没启动
```bash
ps -ef | grep KmPermitServer
# 正常：root  12345  1  0 10:00 ?  00:00:01 ./KmPermitServer_Linux KmServer
# 无输�?�?确实没启�?```

#### 步骤 2：直接运行看报错
```bash
# 不通过脚本，直接运�?./KmPermitServer_Linux KmServer

# 常见报错：缺少库文件
# error while loading shared libraries: libQt5Core.so.5: cannot open shared object file
# 解决�?export LD_LIBRARY_PATH=$PWD/lib:$LD_LIBRARY_PATH
./KmPermitServer_Linux KmServer
```

#### 步骤 3：检�?Glibc 版本
```bash
ldd --version
# 要求 >= 2.31

# 如果版本过低，替�?libstdc++
cd lib
rm libstdc++.so.6
ln -s libstdc++.so.6.0.28 libstdc++.so.6
```

#### 步骤 4：检查授权文�?```bash
# 检�?.lic 是否存在和内�?ls -la kmsoft.lic
cat kmsoft.lic | head -20

# 检�?/tmp 授权缓存
ls -la /tmp/mmfft00.klk
strings /tmp/mmfft00.klk | grep -i "softid\|expire\|202"
```

#### 步骤 5：查看日�?```bash
# 主日�?cat KmPermitServer.log
# 轮转日志
cat KmPermitServer_0.txt
cat KmPermitServer_1.txt
cat KmPermitServer_3.txt
# 搜索错误
grep -i "error\|fail\|licence\|license" KmPermitServer.log
```

#### 步骤 6：检查端�?```bash
netstat -tlnp | grep 5600
# �?ss -tlnp | grep 5600

# 被占用则修改 KmPermitServer.ini
vi KmPermitServer.ini
# [GeneralSet]
# PortNo=5601
```

#### 步骤 7：检查权�?```bash
ls -la KmPermitServer_Linux
chmod +x KmPermitServer_Linux
chmod +x KmPermitServer_Linux_Server.sh
```

#### 步骤 8：检查硬件变�?```bash
# 硬件变更会导致授权失�?cat /proc/cpuinfo | grep "model name" | head -1
fdisk -l | grep "Disk /dev"
ip link show | grep "ether"
# 如果硬件变更，需重新申请 .lic
```

#### Linux 服务端常见错误速查

| 错误信息 | 原因 | 解决 |
|---------|------|------|
| `error while loading shared libraries` | 缺少 Qt/libstdc++ | `export LD_LIBRARY_PATH=$PWD/lib:$LD_LIBRARY_PATH` |
| `GLIBCXX_3.4.21 not found` | Glibc 版本过低 | 替换 `lib/libstdc++.so.6` |
| `read licence error` | 授权过期/硬件变更 | 检�?`.lic`，重新申�?|
| `cannot bind to port 5600` | 端口被占�?| 修改 `KmPermitServer.ini` �?`PortNo` |
| `Permission denied` | 无执行权�?| `chmod +x KmPermitServer_Linux` |
| 无输出直接退�?| 缺少依赖�?| `ldd ./KmPermitServer_Linux` 检查依�?|
| 有进程但客户端连不上 | 防火�?端口未开�?| `firewall-cmd --add-port=5600/tcp --permanent` |

---

### D.4 Java 客户端连不上加密�?- 完整排查

#### 步骤 1：确认服务端是否正常
```bash
# 在服务端机器上检�?ps -ef | grep KmPermitServer
netstat -tlnp | grep 5600
# 必须有进�?+ 端口监听
```

#### 步骤 2：测试网络连通�?```bash
# 在客户端机器上测�?ping 192.168.1.100

# 测试端口连通�?telnet 192.168.1.100 5600
# �?nc -vz 192.168.1.100 5600
# 成功：Connection to 192.168.1.100 5600 port succeeded!

# 如果不通，检查防火墙
# Linux 服务端：
firewall-cmd --list-ports
firewall-cmd --add-port=5600/tcp --permanent
firewall-cmd --reload
```

#### 步骤 3：用 Java 客户端直接测试（绕过 CLOUD�?```bash
# 直接用命令行测试，排�?CLOUD 配置问题
java -jar KmEncryptCardClient.jar -s 192.168.1.100 -p 5600 --soft-id 4 -v

# 如果直接测试成功，但 CLOUD 连不�?�?CLOUD 配置问题
# 如果直接测试也失�?�?网络或服务端问题
```

#### 步骤 4：确�?CLOUD 客户�?mode 配置
```bash
# 查看 CLOUD 配置
cat /path/to/CLOUD/Run/config/wpConfig.yml | grep -A 5 "EncryptionCard"
# kmwp.encryption.EncryptionCard.mode: 2  # 2=Java（默认）
# kmwp.encryption.EncryptionCard.mode: 1  # 1=原生

# 尝试切换 mode 测试
vi /path/to/CLOUD/Run/config/wpConfig.yml
# �?mode 改为 1 �?2 分别测试
# 重启 CLOUD 服务
```

#### 步骤 5：检查授权是否足�?```bash
java -jar KmEncryptCardClient.jar -s 192.168.1.100 -p 5600 --soft-id 4 --product-info 1 --json
# 返回中查�?maxUsers �?usedNodes
# 如果 usedNodes >= maxUsers �?授权节点已满
```

#### 步骤 6：调整超时参数（网络不稳定时�?```bash
cat > config.properties << EOF
server.ip=192.168.1.100
server.port=5600
soft.id=4
mode=session
connect.timeout=15000
request.timeout=30000
heartbeat.interval=30000
heartbeat.timeout=35000
max.reconnect=5
reconnect.delay=2000
EOF

java -jar KmEncryptCardClient.jar -c config.properties
```

#### Java 客户端常见错误速查

| 错误信息 | 原因 | 解决 |
|---------|------|------|
| `Connection refused` | 服务端没启动或端口错�?| 检查服务端进程和端�?|
| `Connection timed out` | 网络不通或防火�?| 检�?ping/telnet，开放防火墙 |
| `No route to host` | IP 地址错误 | 确认服务�?IP |
| `登录失败：无允许登录节点` | 授权用完 | 等待释放或增加授�?|
| `登录失败：有效时间之外` | 授权过期 | 检�?`.lic` 有效�?|
| `向客户端发送数据失败` | 服务端内部错�?| 重启服务端，检查日�?|
| 返回空结�?| 参数错误 | 检�?`soft.id` �?`permit.number` |
| 长连接频繁断开 | 心跳参数不合�?| 调大 heartbeat.timeout |
| CLOUD 中报错但命令行正�?| CLOUD 配置问题 | 检�?`wpConfig.yml` �?mode �?IP |

---

### D.5 快速诊断脚本（Linux 加密卡）

```bash
#!/bin/bash
# 加密卡诊断脚�?- save as check_encrypt.sh
echo "=== 加密卡服务端诊断 ==="
echo ""
echo "1. 检查进程："
ps -ef | grep KmPermitServer | grep -v grep
echo ""
echo "2. 检查端口："
ss -tlnp | grep 5600 || netstat -tlnp | grep 5600
echo ""
echo "3. 检查日志（最�?0行）�?
tail -n 10 KmPermitServer.log 2>/dev/null || echo "无日志文�?
echo ""
echo "4. 检查授权文件："
ls -la kmsoft.lic 2>/dev/null || echo "无授权文�?
echo ""
echo "5. 检�?Glibc 版本�?
ldd --version | head -1
echo ""
echo "6. 检查依赖库�?
ldd ./KmPermitServer_Linux 2>/dev/null | grep "not found" || echo "所有依赖正�?
echo ""
echo "7. 检�?/tmp 授权缓存�?
ls -la /tmp/mmfft00.klk 2>/dev/null || echo "无缓存文�?
echo ""
echo "8. 检查防火墙�?
firewall-cmd --list-ports 2>/dev/null || iptables -L -n 2>/dev/null | grep 5600 || echo "请手动检查防火墙"
echo ""
echo "诊断完成�?
```

---
