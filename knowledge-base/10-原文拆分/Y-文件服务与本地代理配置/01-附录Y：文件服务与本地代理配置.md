---
type: reference
component: 数据库
scenario: 01-附录Y：文件服务与本地代理配置
priority: P2
tags:
- 数据库
- 01-附录Y：文件服务与本地代理配置
- reference
status: active
---

## 附录 Y：文件服务与本地代理配置

### Y.1 文件服务配置

#### 配置文件：run/config/wpFileConfig.ini
```ini
[WPFILESERVICE]
MasterURL = 192.168.80.2:9100    ; 主服务地址（IP:端口�?IsMaster = TRUE                   ; 是否为主文件服务（文件柜所在机�?TRUE�?Version = ABCD
CurrentRoot = A
A = /home/CLOUD/data              ; 文件存储目录（Windows样例：A=D:\data�?```

#### 配置文件：run/config/wpConfig.yaml
```yaml
SecureConfig:
  # 是否启动本地文件服务�?=不启�? 4=启动�?  startFileService: 4
```

**规则**：文件柜所在服务器配置�?4，其余配置为 1。共用一台则配置�?4�?
### Y.2 文件服务连接失败排查

1. 检�?wpFileConfig.ini �?MasterURL 是否正确
2. 检�?wpConfig.yaml �?startFileService 配置
3. 确保文件服务配置唯一

### Y.3 5.0 版本文件服务启动报错

**问题 1**：文件数据服务（70000）配置缺�?- 解决：数据库配置文件中增加文件数据服务配�?
**问题 2**�?文件服务主服务数据库配置校验错误"
- 原因：文件柜 IP 或存储目录变�?- 解决：在 file 库执�?```sql
delete from file_server_state;
commit;
```

### Y.4 可视化文件下载显�?0KB

- 检查文件柜配置是否正确
- ext 文件夹下只能保留最新版本程�?- 确保只启动了一个文件服�?
### Y.5 本地代理配置

#### 环境配置
1. wpConfig.yaml �?`kmwp.init.WpInit.webServerInfo` 后增加：
```yaml
wpWebSocketServer: 12361|12361
```

2. 开启批量上传文件接口：
   - 登录 webApiServer 页面
   - Web 服务接口中勾�?批量上传文件"
   - 点击【开�?Web 服务接口�?   - 关闭身份验证

#### 启动本地代理
```bash
# 将所有依�?jar 和本地代�?jar 放到同一目录
java -jar wpWebSocketSvr.jar
```

#### wpConfig.yaml 默认配置
```yaml
# 第三�?app 通信等待回调时间（毫秒）
kmwp.helper.thirdApp.WebSocketDpMsg.dplResMsgWaitTime: 60000
# 解压字节码（Windows=GBK, Linux=utf-8�?kmwp.config.KMConfig.fileCharset: GBK
# 日志输出级别
kmwp.wpcom.KMDP.DEBUG: 7
```

#### 本地代理无法启动
- JDK 需 1.8 以上
- 检查依�?jar 包是否都存在
- 同个 jar 包只能有一个版�?
#### 本地代理上传文件注意事项
- 不支持文件名带英�?`+` 号（会被转义成空格）
- 导入文件放在一个文件夹里，不要放桌面或根目�?- 如有数量限制，修改配�?
---
