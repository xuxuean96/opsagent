---
type: reference
component: DDB
scenario: 01-三·补：DDB配置项完整参考（逐条详解
priority: P2
tags:
- DDB
- 01-三·补：DDB配置项完整参考（逐条详解
- reference
status: active
---

# 01-三·补：DDB配置项完整参考（逐条详解

## 三·补：DDB 配置项完整参考（逐条详解�?
> 以下内容来自《DDB配置文件使用说明》和《DDB组件使用说明�?KM20080006/SUS001/V003.01)全文提取�?
### 3A.1 配置集合根节�?
```xml
<配置集合 支持的数据源类型="CATIA;CATIA_2D" 配置版本="1">
```

| 属�?| 必填 | 说明 |
|------|------|------|
| `支持的数据源类型` | �?| 多个用分�?`;` 分隔，取值见下表 |
| `配置版本` | �?| PDM432 以后新增。`1`=新方式，无此属�?老方�?|

**支持的数据源类型取�?*�?`READFILESYSTEM`、`EXCELTRANSLATOR`、`WORDTRANSLATOR`、`NOMALTRANS`（Excel结构数据）、`CAXA_2D`、`AUTOCAD_2D`、`SOLIDWORKS`、`SOLIDWORKS_2D`、`PROE`、`PROE_2D`、`INVENTOR`、`INVENTOR_2D`、`UG_SOLIDEDGE`、`UG_SOLIDEDGE_2D`、`CATIA`、`CATIA_2D`、`PROTELTRANSLATOR`、`DDBXML`、`ZW3D`、`ALTIUM_PCB`

### 3A.2 系统配置（`<系统配置>`�?
```xml
<系统配置 名称="扩展部件">
    <接口>DataPsr.CommonDataPsr</接口>     <!-- 必须�?-->
    <接口>KmDDBDWG.DwgBom</接口>            <!-- CAD读写组件 -->
    <接口>DDBDplPsr.DplProcessor</接口>     <!-- DPL处理组件 -->
</系统配置>
```

| 属�?| 说明 |
|------|------|
| `名称` | 必须�?`扩展部件` |

**接口（组�?ProgID）取�?*�?
| ProgID | 用�?|
|--------|------|
| `DataPsr.CommonDataPsr` | 通用数据处理组件�?*必须配置**�?|
| `DDBDplPsr.DplProcessor` | DPL 属性处理组�?|
| `ReadFileSystemAttr.ReadSystem` | 普通文件导�?|
| `ExcelTranslator.DDBExcel` | Excel 导入 |
| `WordTranslator.DDBWord` | Word 导入 |
| `DDBNomalTrans.InitProcess` | Excel 结构数据导入 |
| `CAXATranslator.CAXAInfo` | CAXA 导入 |
| `KmDDBdwg.DwgBom` | AutoCAD 导入 |
| `SWTranslator.DDBSWBominfo` | SolidWorks 导入 |
| `DDBProeTranslator.ProeRW` | ProE 导入 |
| `InvTranslator.InvRW` | Inventor 导入 |
| `UgSeTranslator.UgSeRW` | UG/SolidEdge 导入 |
| `CATIATranslator.CATIARW` | CATIA 导入 |
| `ProtelTranslator.DDBPt` | Protel 导入 |
| `EBOMTranslator.EBOMRW` | 轻量化导�?|
| `EBOMDriver.EBOMRW` | 表缓存轻量化导入 |
| `ZW3DDriver.ZWRW` | 中望3D 2023 导入 |
| `ZWTranslator.ZWRW` | 中望3D 2025 导入 |

### 3A.3 处理配置完整列表�?6种）

> 所有处理配置按配置文件中的**顺序依次执行**，可重复配置、重复执行�?
#### 3A.3.1 属性对应（**必须配置，否则报�?*�?
作用：将原始 XML 中的属性名映射�?part 节点上的目标属性名�?
```xml
<处理配置 名称="属性对�? 属性对应顺�?"文件系统属�?文档属�?>
    <文件系统属�?创建时间="创建时间" 短文件名="名称/\代号"/>
    <文档属�?图幅="图幅" 作�?"作�? 标题="名称"/>
    <标题栏属�?名称="名称" 材料="代号后缀/\材料" 代号="代号"/>
    <明细栏属�?名称="名称" 代号="代号"/>
    <模型属�?质量="质量"/>
    <结构属�?状�?"备注"/>
    <参数表属�?名称="名称" 代号="代号"/>
</处理配置>
```

| 属�?| 说明 |
|------|------|
| `属性对应顺序` | 可动态指定优先级，多个用 `;` 分隔。剩余属性按 XML 排列顺序确定优先�?|

**子元�?*（等号左�?源属性名，等号右�?目标 part 属性名）：

| 子元�?| 适用 | 说明 |
|--------|------|------|
| `<文件系统属�?` | 2D/3D | 文件名、修改时间、创建时间、短文件�?|
| `<文档属�?` | 2D/3D | 作者、标题、主题、文档号、数�?|
| `<标题栏属�?` | 2D | 标题栏单元格属�?|
| `<明细栏属�?` | 2D | 明细栏属�?|
| `<模型属�?` | 3D | 质量、材料等模型属�?|
| `<参数表属�?` | 2D/3D | 参数�?族成员属�?|
| `<结构属�?` | 2D/3D | 结构属性（状态等�?|

**优先�?*（未指定 `属性对应顺序` 时，由低到高）：
- 2D：文件系统属�?< 文档属�?< 明细栏属�?< 标题栏属�?< 参数表属�?- 3D：文件系统属�?< 文档属�?< 模型属�?< 参数表属�?
**特殊语法**�?- `材料="代号后缀/\材料"` �?�?代号后缀"�?材料"两个字段（`\` 为分隔符�?- `短文件名="名称/\代号"` �?�?名称"�?代号"（`/` 为或分隔符）

#### 3A.3.2 组装结构�?
```xml
<处理配置 名称="组装结构�? 组装属�?"代号"/>
```

| 属�?| 说明 |
|------|------|
| `组装属性` | 决定用什么属性来生成装配关系。可�?Part/childPart �?*任意字段** |

作用�?- 针对没有父子关系的原�?XML（一般二维图纸）进行结构树装�?- 主要�?childPart �?refId 赋�?- **`p_` 前缀**：在子节点中配置 `p_代号`（`p_` + 父节点字段名），可将父节点字段反映到本节点中，用于代号加前后缀的组�?
#### 3A.3.3 生成装配结构

```xml
<处理配置 名称="生成装配结构" 组装属�?"代号">
    <明细栏子对象属�?名称="名称" 代号="代号"/>
</处理配置>
```

| 属�?| 说明 |
|------|------|
| `组装属性` | 父子关系关键字，可为任意字段 |

| 子元�?| 说明 |
|--------|------|
| `<明细栏子对象属�?` | `=`左边=写入�?part 的属性名，`=`右边=childPart 中的属性名 |

规则�?- **仅对二维模型有效**
- 不应�?组装结构�?同时使用
- 必须�?属性对�?之后
- 原理：如�?part 的组装属性值与 childPart 相同→将 childPart �?refId 改为对应 part �?id；找不到→生成无�?part 节点

#### 3A.3.4 属性合�?
```xml
<处理配置 名称="属性合�? 覆盖="�? 合并到零�?"�? 保留属�?"数量;材料"/>
```

| 属�?| 默认�?| 说明 |
|------|--------|------|
| `覆盖` | �?| `是`：目标节点相同属性被覆盖；`否`：保留目标原�?|
| `合并到零件` | �?| `是`：childPart→part（明细栏→标题栏）；`否`：part→childPart |
| `保留属性` | �?| 保留原节点上的字段不合并，多个用 `;` 分隔 |

#### 3A.3.5 同义词对�?
```xml
<处理配置 名称="同义词对�?>
    <同义�?对照="名称">Name;名字</同义�?
    <同义�?对照="代号">图表;零部件图�?/同义�?
</处理配置>
```

| 子元�?| 说明 |
|--------|------|
| `<同义�?对照="目标�?>` | 节点值为源名列表，多个用 `;` 分隔。先遇到的属性先对照，后遇到的不处理 |

#### 3A.3.6 产生零件

```xml
<处理配置 名称="产生零件"/>
```

作用：对 childPart �?refId=-1（无对应零件）的，产生一�?part，使 part �?id 与该 childPart �?refId 相同�?
#### 3A.3.7 保存文件

```xml
<!-- XML 格式 -->
<处理配置 名称="保存文件">
    <文件 类型="0">c:\temp.xml</文件>
</处理配置>

<!-- PDM 中间文件 -->
<处理配置 名称="保存文件">
    <文件 类型="pdm4" 条件="[@id=&quot;1&quot;]">c:\temp.xml</文件>
</处理配置>
```

| 属�?| 说明 |
|------|------|
| `类型` | `0`（默认）=XML 格式；`pdm4`=PDM 中间文件格式 |
| `条件` | XPath 格式，指明输出哪�?part 节点（不�?`part` 节点名前缀�?|

#### 3A.3.8 图纸属性拆�?
```xml
<处理配置 名称="图纸属性拆�?>
    <拆分属�?名称="第页">
        <属�?名称="第页" 拆分类型="数字"/>
    </拆分属�?
    <拆分属�?名称="共页">
        <属�?名称="共页" 拆分类型="数字"/>
        <属�?名称="单位" 拆分类型="非数�?/>
    </拆分属�?
</处理配置>
```

拆分类型：`数字`、`非数字`

#### 3A.3.9 DOCUMENT 属性对�?
```xml
<处理配置 名称="DOCUMENT属性对�? 属性对应顺�?"文件系统属�?文档属�?>
    <文件系统属�?创建时间="创建时间"/>
    <文档属�?图幅="图幅"/>
</处理配置>
```

注意：仅�?437 及以上的 DDB 框架支持。把指定属性对应到 document 节点�?
#### 3A.3.10 数量栏处�?
```xml
<处理配置 名称="数量栏处�? 单位="�?�? 非法数量值写为结构属�?"备注"/>
```

| 属�?| 说明 |
|------|------|
| `单位` | PDM 中定义的单位，多个用 `;` 分隔 |
| `非法数量值写为结构属性` | 数量不符合要求时，写入的结构属性名 |

#### 3A.3.11 同层零件数量合并

```xml
<!-- DataPsr.dll 1,1,0,5 及以前（需配合 DPL 脚本�?->
<处理配置 名称="属性处�?>
    <DPL脚本 新增属�?"数量" 对象类型="childPart">
        数量=1;
    </DPL脚本>
</处理配置>
<处理配置 名称="同层零件数量合并"/>

<!-- DataPsr.dll 1,1,0,5 以后（不需�?DPL 脚本�?->
<处理配置 名称="同层零件数量合并"/>
```

仅限三维图纸导入。二维图纸自动提取明细栏数量，不需要此配置�?
#### 3A.3.12 重置节点 ID

```xml
<处理配置 名称="重置节点ID"/>
```

导入 DDBXML 格式时，把关联的 ID 从字符串转换为整数�?
#### 3A.3.13 属性处理（DPL�?
```xml
<处理配置 名称="属性处�?>
    <DPL脚本 新增属�?"类型/|\代号" 删除属�?"备注/|\材料" 对象类型="part">
        if(find(代号,"-00")>=0)
        {
            类型="部件";
        }
    </DPL脚本>
    <DPL脚本 删除属�?"备注" 对象类型="childPart">
        if(find(代号,"-00")>=0) { 类型="部件"; }
    </DPL脚本>
</处理配置>
```

| DPL 脚本属�?| 说明 |
|-------------|------|
| `新增属性` | DPL 执行前新增的字段，多个用 `/|\` 分隔。已存在不改，不存在赋空�?|
| `删除属性` | DPL 执行后删除的字段，多个用 `/|\` 分隔 |
| `对象类型` | `part`（默认）�?`childPart` |

**DPL 脚本函数**�?- `find(属�? "子串")` �?返回位置（≥0 找到�?1 未找到）
- `left(属�? n)` �?取左�?n 个字�?- `triml(属�?` �?去左边空�?- `trimr(属�?` �?去右边空�?- `result=true` �?删除结点条件返回�?
#### 3A.3.14 删除结点（DPL�?
```xml
<处理配置 名称="删除结点">
    <DPL脚本 新增属�?"代号/|\名称" 对象类型="childPart">
        if(代号=="" && 名称=="")
        {
            result=true;
        }
    </DPL脚本>
</处理配置>
```

`result` 初始值为 `false`，当 DPL 返回 `result=true`（数�?1）时删除该子节点�?
#### 3A.3.15 中间文件处理（DPL�?
```xml
<处理配置 名称="中间文件处理">
    <DPL脚本 新增属�?"" 对象类型="document">
        (脚本内容)
    </DPL脚本>
</处理配置>
```

`对象类型`可为任何有效�?XPath�?- `document` �?处理所�?document 节点
- `document/文档属性` �?处理所有文档属性节�?- `part/列` �?处理所有列节点
- `document|part/relation` �?�?`|` 处理多种节点

#### 3A.3.16 测试配置

```xml
<处理配置 名称="测试配置"/>
```

检�?DDB 处理组件是否可以正常读取配置文件�?
### 3A.4 处理配置执行顺序

| 顺序 | 配置 | 说明 |
|------|------|------|
| 1 | 图纸属性拆�?| 处理共页、第�?|
| 2 | 属性对应、属性处�?| 处理叶子节点 |
| 3 | 数量栏处�?| 处理单位 |
| 4 | 生成装配结构、组装结构树、产生零�?| 产生结构 |
| 5 | 同义词对�?| 属性名替换（没有严格要求） |

### 3A.5 读写配置完整详解

#### AutoCAD（坐标模式）

```xml
<读写配置 名称="ACAD" 企业标识="北京二七机车�?>
    <默认字段>文件�?/默认字段>
    <默认字段>图幅</默认字段>
    <默认字段>标识�?/默认字段>
    <标识�?内容="北京二七机车�? 坐标="-103,33,-53,45">
        <标题�?
            <单格 名称="名称" 坐标="-103,12,-53,33"/>
            <单格 名称="代号" 坐标="-53,33,0,45"/>
        </标题�?
        <明细�?坐标="-180,45" 表头�?"7" 行高="7">
            <�?名称="序号" 宽度="8"/>
            <�?名称="代号" 宽度="40"/>
            <�?间隔="0" 垂直坐标="45"/>
        </明细�?
        <参数�?名称="系列A" 方位="右上" 标志�?"dH9"
                 坐标="-10,0" 表头�?"9" 行高="7"/>
    </标识�?
</读写配置>
```

| 元素 | 属�?| 说明 |
|------|------|------|
| `<默认字段>` | �?| 只支�?`文件名`、`图幅`、`标识格` 三种 |
| `<标识�?` | `内容` | 标识格内容（企业标识�?|
| | `坐标` | `左下X,左下Y,右上X,右上Y`（相对于逻辑原点�?|
| `<单格>` | `名称` | 格子名称 |
| | `坐标` | `左下X,左下Y,右上X,右上Y` |
| `<明细�?` | `坐标` | 第一块明细栏左下角坐�?|
| | `表头高` | 表头高度 |
| | `行高` | 每行高度 |
| `<�?` | `名称` | 列名�?|
| | `宽度` | 该列宽度 |
| `<�?` | `间隔` | 与右边块的间�?|
| | `垂直坐标` | 明细栏左下角 Y 坐标 |
| `<参数�?` | `名称` | 参数表名�?|
| | `方位` | 目前只支�?`右上` |
| | `标志格` | 标志格内�?|
| | `坐标` | 左下角坐标（以标志格左下角为原点�?|
| | `表头高`/`行高` | 高度配置 |

**标识格配置方�?*�?1. 测量标题栏长�?/ 标准长度(180) = 比例
2. 以右图框角点为原点，标注标识格块的左下角及右上角坐标（除以比例）
3. 精确到小数点�?3 �?
**注意事项**�?- 标题栏格子中有分隔线**可获�?*内容
- 明细栏中有分隔线**不能获得**内容
- 坐标模式和块模式**不能同时配置**
- 明细栏多块时，即使第二块无表头，垂直坐标也应假定有表�?
#### AutoCAD（块模式�?
| 配置�?| 块模式说�?|
|--------|-----------|
| 标题�?| 指定标题栏块的块�?|
| 明细�?| 明细栏标题项块名 |
| �?| 明细栏行编辑块名 |
| �?| 多栏时各栏落差和间隔（同坐标模式�?|
| 参数�?| 参数表块同明细栏块，方位定死为左�?|

#### Excel 结构数据（NOMALTRANS�?
```xml
<读写配置 数据源类�?"NOMALTRANS"
           保存文件="E:\Excel.xml"
           展开方式="1"
           读取类型="DATABASE"
           读取表名="Sheet1"
           列名所在行="1"
           层号="层次"
           装配规则="所属部件的代号=代号">
    <对象标识>
        <对象>代号</对象>
    </对象标识>
    <精度>
        <保留精度 列名="数�? 保留位数="2" 格式�?"�?/>
    </精度>
</读写配置>
```

| 属�?| 说明 |
|------|------|
| `保存文件` | Xls 展开中间文件路径（便于查错） |
| `展开方式` | `1`=以层次号为关键字展开；`2`=以代号和所属部件代号为关联展开；`3`=方式2扩展（忽略重�?childPart�?|
| `读取类型` | `COM`=COM 接口方式；`DATABASE`=数据库方式（大数据量推荐�?|
| `读取表名` | 指定 xls 表名。未指定则取第一�?|
| `列名所在行` | 列标题所在行�?|
| `层号` | 展开方式=1 时配置。层次号列标题名 |
| `装配规则` | 展开方式=2 时配置。`所属部件的列名=零件的列名` |

**`<对象标识>`**�?```xml
<对象标识>
    <对象>代号</对象>
</对象标识>
```
- 用于判断对象的唯一性（即零部件唯一标识�?- 对象标识数量**最多不超过 3 �?*，超�?3 个按 3 个处�?- 如果配置�?代号"但数据源中无"代号"列，所有对象会合并成一�?
**`<精度>`**�?```xml
<保留精度 列名="数�? 保留位数="2" 格式�?"�?/>
```
- `保留位数`：小数点后位数，最�?1，最�?6
- `格式�?"�?`：删除小数点后面�?0

#### ProE 读写配置

- ProE 工程图按**表格行和�?*标识单元格（不是按坐标）
- 一张工程图可有多个表格，需先确定是哪个表格（需给定表格唯一标识�?- 二维工程图表格的行和列要区分降序和升�?- **三维导入时必须配置参数表的代号和名称**
- 信息提取方式：`"文件"`=破解方式读取（速度快）；其�?ProE 自身 API（准确）

#### Catia 读写配置

- Catia �?UG_SOLIDEDGE 三维文件导入**不存在读写配�?*
- 二维工程图：标题栏需单元格坐标，明细栏需表头高、行高和每列宽度

#### Inventor 二维工程�?
- 只需给出标题栏单元格坐标和图纸标识格坐标
- **不需要明细栏任何信息**
- 标识格不能选带 `<>` 的文本框
- 坐标测量：编辑定�?�?模型浏览�?�?量取

#### SolidEdge 读写配置

```xml
<读写配置 名称="SOLIDEDGE" 分析�?"Revision">
    <输出字段 Title="标题" Subject="主题" Author="作�? Manager="管理�?/>
</读写配置>
```
- `分析器`：`Revision`=�?Revision Manager（性能好）；`Application`=用应用程�?- 不配�?`输出字段` 时输出所有属�?
### 3A.6 零部件唯一标识详解

DDB XML 中对象唯一性通过以下机制实现�?
| 机制 | 说明 |
|------|------|
| part 节点 `id` 属�?| 零件的唯一标识 |
| childPart 节点 `refId` 属�?| 引用�?part �?id |
| `refId=-1` | 表示无对�?part |
| 组装结构�?`组装属性` | 用指定字段值匹配父子关�?|
| 生成装配结构 `组装属性` | 匹配则设�?refId，不匹配则生成新 part |
| `<对象标识><对象>代号</对象>` | Excel 结构数据中判断对象唯一性，最�?3 �?|

### 3A.7 缓存表字段定义（表缓存方式）

| 表名 | 关键字段 | 说明 |
|------|---------|------|
| `Buf_Part` | F_Id, F_ParentId | 零件信息 |
| `Buf_SysDocAttr` | F_Id | 系统文档属�?|
| `Buf_DocAttr` | F_Id | 文档属�?|
| `Buf_Sheet` | F_Id | 图纸信息 |
| `Buf_StructRelation` | F_Id, F_ParentId | 结构关系 |
| `Buf_ModelAttr` | F_Id | 模型属�?|
| `Buf_StructAttr` | F_Id | 结构属�?|
| `Buf_TitleAttr` | F_Id | 标题栏属�?|
| `Buf_ItemAttr` | F_Id | 明细栏属�?|
| `Buf_ParaTable` | F_Id | 参数�?|
| `Buf_ParaTableRowData` | F_Id | 参数表行数据 |

关系规则：`F_Id` 为本表主键，`F_ParentId` 为父表外键�?
### 3A.8 特殊语法汇�?
| 语法 | 用�?| 示例 |
|------|------|------|
| `;` | 多值分�?| 保留属性、属性对应顺序、单位、同义词源名 |
| `/|\` | DPL 多字段分�?| 新增属�?"代号/|\名称/|\材料" |
| `/\` | 取多个字�?| 材料="代号后缀/\材料" |
| `/` | 取代默认分隔 | 短文件名="名称/\代号" |
| `p_` | 父节点字段前缀 | p_代号（组装结构树中） |
| `\|` | XPath 多节点分�?| document\|part/relation |
| `=` 左右 | 属性对�?| �?�?列名，右=目标 part 属性名 |

### 3A.9 disposetype 取�?
导入时控制创建新版本还是覆盖�?
| �?| 含义 |
|----|------|
| `create` | 创建新版�?|
| `refer` | 引用 |
| `refer_new` | 引用最新版�?|
| `refer_release` | 引用最新发布版�?|
| `refer_notrelease` | 引用最新未发布版本 |
| `rewrite` | 更新 |
| `rewrite_notrelease` | 更新最新未发布版本 |

```xml
<处理配置 名称="属性处�?>
    <DPL脚本 新增属�?"disposetype" 对象类型="part">
        if(备注=="标准�?)
            disposetype="refer_notrelease";
    </DPL脚本>
</处理配置>
```

### 3A.10 族信息读取配�?
**二维图纸**（需配置两个位置）：
1. 处理配置中加 `<参数表属�?名称="名称" 代号="代号"/>`
2. 读写配置中加 `<参数表坐�?`（可配多个）

**三维图纸 ProE**（仅需处理配置）：
```xml
<参数表属�?pindex="物料�? cname="名称" 实例�?"代号"/>
```
配合�?```xml
<文档属�?CNAME="名称" 代号后缀="代号后缀" PINDEX="代号"/>
```

### 3A.11 �?CAD 组件注册名称对照

| CAD 软件 | 读写配置名称 | 组件 | 类名 |
|----------|-------------|------|------|
| AutoCAD | `ACAD` | kmdwgbom.dll | KmDwgBom.DwgBom |
| SolidEdge | `SOLIDEDGE` | DDBSeBom.dll | DDBSeBom.SeBom |
| AvidmXML | `AVID` | DDBAVIDMXML.dll | DDBAVIDMXML.AvidBom |
| TiGems | `GEMS` | DDBGEMS.dll | DDBGEMS.GemsBom |
| KM3DCAD | `KM3D` | DDBKM3DCAD.dll | DDBKM3DCAD.Km3dBom |
| Cimatron | `CIMATRON` | DDBCimatronBom.dll | DDBCimatronBom.CimatronBom |
| CAXA3D | `CAXA3D` | KMCaxaBom.dll | KmCaxaBom.Caxa3DBom |

---
