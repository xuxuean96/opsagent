---
type: reference
component: DDB
scenario: 01-附录K：签字功能完整指
priority: P2
tags:
- DDB
- 01-附录K：签字功能完整指
- reference
status: active
---

# 01-附录K：签字功能完整指

## 附录 K：签字功能完整指�?
### L.1 DWG 文字签字

#### 配置方法
1. 在XML配置文件中配置签字坐标（与标题栏坐标配置方式相同�?2. 在update.xml中指定签字内容和位置
3. 运行Update模式

#### 常见错误
- 签字位置错位：标识格跨格导致
- 解决：更换标识格，确保不跨格
- 标识格不能为"阶段"�?版本"（因为它们是整体�?
### L.2 DWG 图片签字

#### 配置方法
1. 配置图片签字坐标
2. 在update.xml中指定图片路�?3. 运行Update模式

#### Windows Server 2016 特殊问题
- 现象：图片签字显示为bmp图标，不显示图片
- 原因：操作系统注册表被修�?- 解决：修改注册表（具体键值见QA文档�?- 注意：Windows Server 2016裸机正常，是安装某些软件后导�?
### L.3 Word 文字签字

#### 配置方法
1. 在XML配置中指定表格限定范围（�?1-3"表示�?-3个表格）
2. 在update.xml中指定签字内容和单元�?3. 运行Update模式

#### 常见错误
- 签字签不上：表格限定范围太小
- 解决：扩大表格限定范围，�?1-10"

### L.4 Word 图片签字

#### 配置方法
1. 在update.xml中指定图片路径和插入位置
2. 运行Update模式

#### 常见错误
- 图片签不上但文字可以：组件版本不一�?- 解决：确保DDB目录下所有DLL为同一版本

### L.5 Excel 签字

#### 配置方法
1. 在XML配置中指定工作表和单元格
2. 在update.xml中指定签字内�?3. 运行Update模式

### L.6 SolidWorks 工程图签�?
#### 配置方法
1. 在工程图中添加注释，内容�?$PRPSHEET:{属性名}"
2. 在update.xml中指定属性�?3. 运行Update模式

### L.7 CATIA 工程图签�?
#### 配置方法
1. 配置签字坐标
2. 在update.xml中指定签字内�?3. 运行Update模式

#### 注意
- CATIA工程图签字需要确保文本是注释形式，不是表单格
- 表单格生成的文本DDB框架无法识别

### L.8 Inventor 工程图签�?
#### 配置方法
1. 配置签字坐标
2. 在update.xml中指定签字内�?3. 运行Update模式

#### 签字高度控制
- 如果签字位置定义了文本框，签字高度按文本框大�?- 如果没有定义文本框，签字高度为坐标文本框高度�?.5�?
### L.9 CAXA 签字

#### 配置方法
1. 确保CAXA命令行窗口开启（右键空白处，勾�?命令�?�?2. 配置签字坐标
3. 在update.xml中指定签字内�?4. 运行Update模式

#### 注意
- 签字时CAXA会自动启动，签字完成后自动关�?- 不要手动干预，等待自动完�?
### L.10 中望CAD 签字

#### 配置方法
1. 确保标题栏块中定义了属性（增强属性编辑器中可见）
2. 在update.xml中指定属性�?3. 运行Update模式

#### 关键要求
- 需要签字的单元格必须在块中定义为属�?- 没有定义属性的单元格无法签�?
---
